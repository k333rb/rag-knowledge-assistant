# RAG Knowledge Assistant

A chatbot that answers questions about FastAPI's documentation, grounded
in the actual documentation content, not just general knowledge. Uses
retrieval augmented generation, relevant documentation chunks are
retrieved for each question, and the AI answers only from what was
retrieved, citing its sources.

## Status

Under active development, see commit history for build progress.
